{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "choirloom:score/rational-time-collection/0.1.0",
  "title": "RationalTimeGoldenCollection",
  "version": "0.1.0",
  "description": "Versioned envelope for tests/golden/rational_time.samples.json. Each case carries a 'name', an optional 'note', and a 'value' that is a strict RationalTime object per the item schema, referenced below by its $id (choirloom:score/rational-time/0.1.0). DRAFT / NON-FROZEN; explicit versioned migration is required before evolution, and an ADR is only needed if a frozen decision changes (see docs/rational-time-notes.md).",
  "type": "object",
  "required": ["cases"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "cases": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "value"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "note": {
            "type": "string"
          },
          "value": {
            "$ref": "choirloom:score/rational-time/0.1.0"
          }
        }
      }
    }
  }
}
